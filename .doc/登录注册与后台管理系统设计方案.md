# 登录注册与后台管理系统设计方案

## 1. 概述

本次迭代为 AI 试戴间项目新增用户体系与后台管理功能，包括：
- 用户注册/登录（邮箱 + 用户名密码 + JWT 认证）
- 用户生成次数配额管理
- 推荐图片配置管理
- 后台管理系统（Admin UI）

---

## 2. 技术架构

### 2.1 技术栈

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| **数据库** | MySQL 8.0+ | 关系型数据库 |
| **ORM** | SQLAlchemy + PyMySQL | 保持现有 ORM，更换驱动 |
| **迁移工具** | Alembic | 数据库版本管理 |
| **后端认证** | JWT (python-jose) | 无状态认证，支持过期时间 |
| **密码安全** | passlib + bcrypt | 密码哈希存储 |
| **后端** | FastAPI | 保持现有技术栈 |
| **前端路由** | React Router v6 | SPA 路由管理 |
| **前端状态** | React Context + hooks | 认证状态全局管理 |
| **后台 UI** | shadcn/ui | 与前台保持一致的设计系统 |

### 2.2 目录结构变更

```
pictureCrossing/
├── frontEnd/
│   ├── src/
│   │   ├── routes/
│   │   │   ├── index.tsx          # 路由主文件
│   │   │   ├── protected-route.tsx
│   │   │   └── admin-route.tsx
│   │   ├── pages/
│   │   │   ├── login.tsx
│   │   │   ├── register.tsx
│   │   │   ├── forgot-password.tsx
│   │   │   ├── reset-password.tsx
│   │   │   ├── verify-email.tsx
│   │   │   └── admin/
│   │   │       ├── layout.tsx
│   │   │       ├── dashboard.tsx
│   │   │       ├── users.tsx
│   │   │       └── recommendations.tsx
│   │   ├── components/
│   │   │   ├── admin/             # 后台组件
│   │   │   │   ├── sidebar.tsx
│   │   │   │   ├── header.tsx
│   │   │   │   └── stats-card.tsx
│   │   │   ├── auth/              # 认证组件
│   │   │   │   ├── login-form.tsx
│   │   │   │   └── register-form.tsx
│   │   │   ├── user-menu.tsx      # 用户下拉菜单
│   │   │   ├── quota-display.tsx  # 配额显示
│   │   │   └── recommendation-carousel.tsx
│   │   ├── context/
│   │   │   └── auth-context.tsx
│   │   ├── hooks/
│   │   │   ├── use-auth.ts
│   │   │   └── use-pagination.ts
│   │   ├── services/
│   │   │   ├── auth.ts            # 认证 API
│   │   │   ├── admin.ts           # 管理 API
│   │   │   └── api.ts             # 更新：支持自动刷新
│   │   ├── types/
│   │   │   ├── auth.ts
│   │   │   └── admin.ts
│   │   └── ...
│   └── ...
└── backEnd/
    ├── alembic.ini
    ├── alembic/
    │   ├── env.py
    │   ├── script.py.mako
    │   └── versions/
    │       ├── 001_initial_schema.py
    │       └── 002_add_auth_tables.py
    ├── app/
    │   ├── api/
    │   │   ├── __init__.py
    │   │   ├── auth.py            # 新增：认证接口
    │   │   ├── users.py           # 新增：用户接口
    │   │   ├── admin.py           # 新增：管理接口
    │   │   ├── presets.py         # 更新：增加推荐图
    │   │   ├── generate.py        # 更新：增加认证和配额
    │   │   └── upload.py
    │   ├── core/
    │   │   ├── __init__.py
    │   │   ├── auth.py            # 新增：JWT 工具
    │   │   ├── security.py        # 新增：密码、审计、限流
    │   │   ├── email.py           # 新增：邮件发送（可选）
    │   │   └── init_data.py       # 更新：初始化数据
    │   ├── services/
    │   │   ├── __init__.py
    │   │   └── task_queue.py      # 更新：增加 user_id
    │   ├── models.py              # 大更新：新增用户相关模型
    │   ├── schemas.py             # 大更新：新增认证 Schema
    │   ├── config.py              # 更新：新增 JWT 配置
    │   ├── database.py            # 更新：MySQL 连接池
    │   └── dependencies.py        # 更新：认证依赖
    ├── scripts/
    │   └── migrate_data.py        # 新增：SQLite → MySQL 迁移
    ├── main.py                    # 更新：注册新路由
    ├── requirements.txt           # 更新：新增依赖
    └── .env.example               # 更新：新增配置项
```

---

## 3. 数据库设计

### 3.1 数据模型

```python
# backEnd/app/models.py

import enum
import uuid
from datetime import datetime
from sqlalchemy import (
    Column, String, Integer, DateTime, Text, Boolean,
    Enum as SQLEnum, ForeignKey
)
from sqlalchemy.orm import relationship, declarative_base

Base = declarative_base()

class UserRole(str, enum.Enum):
    USER = "user"
    ADMIN = "admin"

class TaskStatus(str, enum.Enum):
    PENDING = "pending"
    PROCESSING = "processing"
    COMPLETED = "completed"
    FAILED = "failed"

class User(Base):
    """用户表"""
    __tablename__ = "users"

    id = Column(String(36), primary_key=True, default=lambda: str(uuid.uuid4()))
    username = Column(String(50), unique=True, nullable=False, index=True)
    email = Column(String(255), unique=True, nullable=False, index=True)
    hashed_password = Column(String(255), nullable=False)
    role = Column(SQLEnum(UserRole), default=UserRole.USER, nullable=False)

    # 生成次数配额
    quota_total = Column(Integer, default=10, nullable=False)
    quota_used = Column(Integer, default=0, nullable=False)

    # 账号状态
    is_active = Column(Boolean, default=True, nullable=False)
    email_verified = Column(Boolean, default=False, nullable=False)

    # 邮箱验证
    email_verification_token = Column(String(100), nullable=True)
    email_verification_sent_at = Column(DateTime, nullable=True)

    # 密码重置
    password_reset_token = Column(String(100), nullable=True, index=True)
    password_reset_expires_at = Column(DateTime, nullable=True)

    # 活动记录
    last_login_at = Column(DateTime, nullable=True)
    last_activity_at = Column(DateTime, nullable=True)

    # 首次登录强制修改密码
    require_password_change = Column(Boolean, default=False, nullable=False)

    created_at = Column(DateTime, default=datetime.utcnow, nullable=False)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow, nullable=False)

    # 关联
    tasks = relationship("GenerationTask", back_populates="user")
    refresh_tokens = relationship("RefreshToken", back_populates="user", cascade="all, delete-orphan")
    audit_logs = relationship("AuditLog", back_populates="user")
    password_history = relationship("PasswordHistory", back_populates="user", cascade="all, delete-orphan")

class RefreshToken(Base):
    """Refresh Token 表（支持登出/撤销）"""
    __tablename__ = "refresh_tokens"

    id = Column(String(36), primary_key=True, default=lambda: str(uuid.uuid4()))
    user_id = Column(String(36), ForeignKey("users.id"), nullable=False, index=True)
    token = Column(String(255), unique=True, nullable=False, index=True)
    expires_at = Column(DateTime, nullable=False)
    revoked_at = Column(DateTime, nullable=True)
    created_at = Column(DateTime, default=datetime.utcnow, nullable=False)

    user = relationship("User", back_populates="refresh_tokens")

class PasswordHistory(Base):
    """密码历史记录（防止近期重复使用）"""
    __tablename__ = "password_history"

    id = Column(String(36), primary_key=True, default=lambda: str(uuid.uuid4()))
    user_id = Column(String(36), ForeignKey("users.id"), nullable=False, index=True)
    hashed_password = Column(String(255), nullable=False)
    created_at = Column(DateTime, default=datetime.utcnow, nullable=False)

    user = relationship("User", back_populates="password_history")

class AuditLog(Base):
    """审计日志"""
    __tablename__ = "audit_logs"

    id = Column(String(36), primary_key=True, default=lambda: str(uuid.uuid4()))
    user_id = Column(String(36), ForeignKey("users.id"), nullable=True, index=True)
    action = Column(String(50), nullable=False, index=True)
    resource_type = Column(String(50), nullable=True)
    resource_id = Column(String(36), nullable=True)
    old_value = Column(Text, nullable=True)
    new_value = Column(Text, nullable=True)
    ip_address = Column(String(50), nullable=True)
    user_agent = Column(String(255), nullable=True)
    created_at = Column(DateTime, default=datetime.utcnow, nullable=False, index=True)

    user = relationship("User", back_populates="audit_logs")

class RecommendationImage(Base):
    """推荐图配置（统一管理配饰和模特预设）"""
    __tablename__ = "recommendation_images"

    id = Column(String(36), primary_key=True, default=lambda: str(uuid.uuid4()))
    title = Column(String(200), nullable=False)
    description = Column(Text, nullable=True)
    image_url = Column(String(500), nullable=False)
    target_type = Column(String(50), nullable=False, index=True)  # accessory 或 model
    target_value = Column(String(50), nullable=False, index=True)  # 如 bracelet 或 wrist
    sort_order = Column(Integer, default=0, nullable=False)
    is_active = Column(Boolean, default=True, nullable=False, index=True)
    deleted_at = Column(DateTime, nullable=True)
    created_at = Column(DateTime, default=datetime.utcnow, nullable=False)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow, nullable=False)

class PresetScene(Base):
    __tablename__ = "preset_scenes"
    id = Column(String(36), primary_key=True)
    category = Column(String(50), nullable=False, index=True)
    name = Column(String(100), nullable=False)
    image_url = Column(String(500), nullable=False)
    prompt_template = Column(Text)
    sort_order = Column(Integer, default=0)
    created_at = Column(DateTime, default=datetime.utcnow)

class GenerationTask(Base):
    __tablename__ = "generation_tasks"

    id = Column(String(36), primary_key=True)
    status = Column(SQLEnum(TaskStatus), default=TaskStatus.PENDING, index=True)
    accessory_type = Column(String(50), nullable=False)

    accessory_source = Column(String(20))
    accessory_id = Column(String(100))
    accessory_url = Column(String(500))

    model_source = Column(String(20))
    model_id = Column(String(100))
    model_url = Column(String(500))
    model_mask_url = Column(String(500))

    scene_id = Column(String(100))

    lighting = Column(String(50), default="natural")
    prompt = Column(Text)

    result_image_url = Column(String(500))
    error_message = Column(Text)

    # 用户关联
    user_id = Column(String(36), ForeignKey("users.id"), nullable=True, index=True)
    user = relationship("User", back_populates="tasks")

    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
```

### 3.2 MySQL 配置与迁移

#### 3.2.1 数据库连接配置

```python
# backEnd/app/config.py

from pydantic_settings import BaseSettings
from typing import List, Literal, Optional

class Settings(BaseSettings):
    app_name: str = "AI 试衣间 API"
    debug: bool = True

    # MySQL 数据库配置
    database_url: str = "mysql+pymysql://root:password@localhost:3306/ai_fitting_room"
    pool_size: int = 5
    max_overflow: int = 10
    pool_timeout: int = 30
    pool_recycle: int = 3600

    # 文件存储配置
    upload_dir: str = "./uploads"
    result_dir: str = "./results"
    static_dir: str = "./static"
    max_upload_size: int = 10 * 1024 * 1024

    # 限流配置
    rate_limit_enabled: bool = True
    rate_limit_per_minute: int = 5
    rate_limit_per_day: int = 50

    # AI 服务配置
    ai_provider: Literal["mock", "stable_diffusion", "bailian"] = "mock"
    bailian_api_key: str = ""
    bailian_model: str = "qwen-image-2.0-pro"
    bailian_base_url: str = "https://dashscope.aliyuncs.com/api/v1"
    stable_diffusion_api_url: str = "http://localhost:7860"
    public_base_url: str = ""

    # JWT 配置
    jwt_secret_key: str = "your-super-secret-key-change-in-production-please"
    jwt_algorithm: str = "HS256"
    jwt_access_token_expire_minutes: int = 1440
    jwt_refresh_token_expire_days: int = 7

    # 默认配额
    default_quota: int = 10

    # 密码策略
    password_min_length: int = 8
    password_require_uppercase: bool = True
    password_require_lowercase: bool = True
    password_require_digits: bool = True
    password_require_special: bool = False
    password_history_limit: int = 5

    # 邮件配置（可选，不配置则跳过验证）
    smtp_host: Optional[str] = None
    smtp_port: int = 587
    smtp_user: Optional[str] = None
    smtp_password: Optional[str] = None
    from_email: Optional[str] = None

    # 邮箱验证开关
    require_email_verification: bool = False

    # CORS
    cors_origins: List[str] = ["http://localhost:5173", "http://localhost:3000"]

    model_config = {
        "env_file": ".env",
        "env_file_encoding": "utf-8"
    }

def get_settings() -> Settings:
    return Settings()
```

#### 3.2.2 .env.example

```env
# 数据库
DATABASE_URL=mysql+pymysql://root:password@localhost:3306/ai_fitting_room

# JWT
JWT_SECRET_KEY=your-super-secret-key-change-in-production-please
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# 默认配置
DEFAULT_QUOTA=10

# 邮箱验证开关（开发环境建议关闭）
REQUIRE_EMAIL_VERIFICATION=false

# 邮件配置（可选，用于发送验证邮件）
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
FROM_EMAIL=

# 密码策略
PASSWORD_MIN_LENGTH=8
PASSWORD_REQUIRE_UPPERCASE=true
PASSWORD_REQUIRE_LOWERCASE=true
PASSWORD_REQUIRE_DIGITS=true
PASSWORD_REQUIRE_SPECIAL=false
```

#### 3.2.3 database.py (MySQL 连接池)

```python
# backEnd/app/database.py

from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker, Session
from app.config import get_settings

settings = get_settings()

engine = create_engine(
    settings.database_url,
    pool_size=settings.pool_size,
    max_overflow=settings.max_overflow,
    pool_timeout=settings.pool_timeout,
    pool_recycle=settings.pool_recycle,
    pool_pre_ping=True,
    echo=settings.debug,
)

SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)

def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()

def init_db():
    # 使用 Alembic 迁移，不在这里创建表
    pass

def reset_stuck_tasks():
    from app.models import GenerationTask, TaskStatus
    db = SessionLocal()
    try:
        stuck_tasks = db.query(GenerationTask).filter(
            GenerationTask.status == TaskStatus.PROCESSING
        ).all()
        for task in stuck_tasks:
            task.status = TaskStatus.FAILED
            task.error_message = "Task was interrupted by server restart"
        db.commit()
    except Exception as e:
        print(f"重置任务失败: {e}")
        db.rollback()
    finally:
        db.close()
```

#### 3.2.4 Alembic 迁移

```
backEnd/
├── alembic.ini
├── alembic/
│   ├── env.py
│   ├── script.py.mako
│   └── versions/
│       ├── 001_initial_schema.py
│       └── 002_add_auth_tables.py
```

初始化数据库 SQL：

```sql
CREATE DATABASE IF NOT EXISTS ai_fitting_room
  DEFAULT CHARACTER SET utf8mb4
  DEFAULT COLLATE utf8mb4_unicode_ci;
```

---

## 4. 后端 API 设计

### 4.1 认证接口 (`/api/auth`)

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| `POST` | `/register` | 用户注册 | 否 |
| `POST` | `/login` | 用户登录（支持邮箱或用户名） | 否 |
| `POST` | `/refresh` | 刷新 Token | Refresh Token |
| `POST` | `/logout` | 登出 | Access Token |
| `GET` | `/me` | 获取当前用户信息 | Access Token |
| `PUT` | `/me` | 更新当前用户信息 | Access Token |
| `POST` | `/change-password` | 修改密码 | Access Token |
| `POST` | `/send-verification-email` | 发送验证邮件 | 否 |
| `POST` | `/verify-email` | 验证邮箱 | 否 |
| `POST` | `/forgot-password` | 请求重置密码 | 否 |
| `POST` | `/reset-password` | 重置密码 | 否 |

### 4.2 用户接口 (`/api/users`)

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| `GET` | `/quota` | 获取当前用户配额 | Access Token |
| `GET` | `/history` | 获取生成历史（分页） | Access Token |

### 4.3 管理接口 (`/api/admin`)

| 方法 | 路径 | 说明 | 角色 |
|------|------|------|------|
| `GET` | `/stats` | 统计概览 | Admin |
| `GET` | `/users` | 用户列表（分页/搜索/过滤） | Admin |
| `POST` | `/users` | 创建用户 | Admin |
| `GET` | `/users/{id}` | 用户详情 | Admin |
| `PUT` | `/users/{id}` | 更新用户 | Admin |
| `PUT` | `/users/{id}/quota` | 调整配额 | Admin |
| `PUT` | `/users/{id}/toggle-active` | 启用/禁用用户 | Admin |
| `GET` | `/recommendations` | 推荐图列表 | Admin |
| `POST` | `/recommendations` | 创建推荐图 | Admin |
| `GET` | `/recommendations/{id}` | 推荐图详情 | Admin |
| `PUT` | `/recommendations/{id}` | 更新推荐图 | Admin |
| `DELETE` | `/recommendations/{id}` | 删除推荐图 | Admin |
| `POST` | `/recommendations/{id}/move-up` | 上移排序 | Admin |
| `POST` | `/recommendations/{id}/move-down` | 下移排序 | Admin |

### 4.4 公开接口

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| `GET` | `/api/presets/recommendations` | 获取前台展示的推荐图 | 否 |

### 4.5 更新现有接口

| 方法 | 路径 | 变更说明 |
|------|------|----------|
| `POST` | `/api/generate` | 需要认证，扣减配额，关联 user_id |
| `GET` | `/api/generate/{task_id}` | 需要认证，只能查询自己的任务 |

---

## 5. Schema 设计

```python
# backEnd/app/schemas.py

from pydantic import BaseModel, Field, EmailStr, model_validator
from datetime import datetime
from typing import Optional, List, Any, Literal, Generic, TypeVar
import re

# ==================== 通用 ====================

T = TypeVar('T')

class ApiResponse(BaseModel, Generic[T]):
    success: bool = True
    data: Optional[T] = None
    message: Optional[str] = None

class PaginationParams(BaseModel):
    page: int = Field(1, ge=1)
    page_size: int = Field(20, ge=1, le=100)

class SortParams(BaseModel):
    sort_by: Optional[str] = None
    sort_order: Literal["asc", "desc"] = "desc"

class PaginatedResponse(BaseModel, Generic[T]):
    items: List[T]
    total: int
    page: int
    page_size: int
    total_pages: int

# ==================== 认证相关 ====================

class UserRegister(BaseModel):
    username: str = Field(..., min_length=3, max_length=50, description="用户名")
    email: EmailStr = Field(..., max_length=255, description="邮箱")
    password: str = Field(..., min_length=8, max_length=128, description="密码")

    @model_validator(mode="after")
    def validate_password(self):
        from app.config import get_settings
        settings = get_settings()

        password = self.password
        errors = []

        if len(password) < settings.password_min_length:
            errors.append(f"密码长度至少 {settings.password_min_length} 位")

        if settings.password_require_uppercase and not re.search(r'[A-Z]', password):
            errors.append("密码需要包含大写字母")

        if settings.password_require_lowercase and not re.search(r'[a-z]', password):
            errors.append("密码需要包含小写字母")

        if settings.password_require_digits and not re.search(r'[0-9]', password):
            errors.append("密码需要包含数字")

        if settings.password_require_special and not re.search(r'[!@#$%^&*(),.?":{}|<>]', password):
            errors.append("密码需要包含特殊字符")

        if errors:
            raise ValueError("; ".join(errors))

        return self

class UserLogin(BaseModel):
    login: str = Field(..., description="邮箱或用户名")
    password: str = Field(..., description="密码")

class TokenResponse(BaseModel):
    access_token: str
    refresh_token: str
    token_type: str = "bearer"
    expires_in: int

class RefreshTokenRequest(BaseModel):
    refresh_token: str

class UserResponse(BaseModel):
    id: str
    username: str
    email: str
    role: str
    quota_total: int
    quota_used: int
    is_active: bool
    email_verified: bool
    created_at: datetime

    class Config:
        from_attributes = True

class UserUpdateRequest(BaseModel):
    username: Optional[str] = Field(None, min_length=3, max_length=50)
    email: Optional[EmailStr] = None

class ChangePasswordRequest(BaseModel):
    current_password: str
    new_password: str

    @model_validator(mode="after")
    def validate_new_password(self):
        from app.config import get_settings
        settings = get_settings()

        password = self.new_password
        errors = []

        if len(password) < settings.password_min_length:
            errors.append(f"密码长度至少 {settings.password_min_length} 位")

        if settings.password_require_uppercase and not re.search(r'[A-Z]', password):
            errors.append("密码需要包含大写字母")

        if settings.password_require_lowercase and not re.search(r'[a-z]', password):
            errors.append("密码需要包含小写字母")

        if settings.password_require_digits and not re.search(r'[0-9]', password):
            errors.append("密码需要包含数字")

        if errors:
            raise ValueError("; ".join(errors))

        return self

class SendVerificationEmailRequest(BaseModel):
    email: EmailStr

class VerifyEmailRequest(BaseModel):
    email: EmailStr
    token: str

class ForgotPasswordRequest(BaseModel):
    email: EmailStr

class ResetPasswordRequest(BaseModel):
    token: str
    new_password: str

    @model_validator(mode="after")
    def validate_new_password(self):
        from app.config import get_settings
        settings = get_settings()

        password = self.new_password
        errors = []

        if len(password) < settings.password_min_length:
            errors.append(f"密码长度至少 {settings.password_min_length} 位")

        if errors:
            raise ValueError("; ".join(errors))

        return self

# ==================== 用户接口 ====================

class QuotaResponse(BaseModel):
    quota_total: int
    quota_used: int
    quota_remaining: int

class TaskHistoryParams(PaginationParams, SortParams):
    status: Optional[str] = None
    accessory_type: Optional[str] = None
    date_from: Optional[datetime] = None
    date_to: Optional[datetime] = None

class TaskHistoryItem(BaseModel):
    id: str
    status: str
    accessory_type: str
    result_image_url: Optional[str]
    created_at: datetime

    class Config:
        from_attributes = True

# ==================== 管理接口 ====================

class AdminStatsResponse(BaseModel):
    total_users: int
    active_users_today: int
    total_generations: int
    generations_today: int
    quota_usage_rate: float

class UserListParams(PaginationParams, SortParams):
    search: Optional[str] = None
    role: Optional[str] = None
    is_active: Optional[bool] = None

class AdminUserResponse(BaseModel):
    id: str
    username: str
    email: str
    role: str
    quota_total: int
    quota_used: int
    is_active: bool
    email_verified: bool
    last_login_at: Optional[datetime]
    created_at: datetime

    class Config:
        from_attributes = True

class AdminUserCreateRequest(BaseModel):
    username: str = Field(..., min_length=3, max_length=50)
    email: EmailStr
    password: str = Field(..., min_length=8)
    role: Literal["user", "admin"] = "user"
    quota_total: int = Field(10, ge=0)
    is_active: bool = True

class AdminUserUpdateRequest(BaseModel):
    username: Optional[str] = Field(None, min_length=3, max_length=50)
    email: Optional[EmailStr] = None
    role: Optional[Literal["user", "admin"]] = None
    is_active: Optional[bool] = None

class AdminUserQuotaUpdateRequest(BaseModel):
    quota_total: int = Field(..., ge=0)

class RecommendationCreateRequest(BaseModel):
    title: str = Field(..., min_length=1, max_length=200)
    description: Optional[str] = None
    image_url: str = Field(..., max_length=500)
    target_type: str = Field(..., pattern="^(accessory|model)$")
    target_value: str = Field(..., min_length=1, max_length=50)
    sort_order: int = 0
    is_active: bool = True

class RecommendationUpdateRequest(BaseModel):
    title: Optional[str] = Field(None, min_length=1, max_length=200)
    description: Optional[str] = None
    image_url: Optional[str] = Field(None, max_length=500)
    target_type: Optional[str] = Field(None, pattern="^(accessory|model)$")
    target_value: Optional[str] = Field(None, min_length=1, max_length=50)
    sort_order: Optional[int] = None
    is_active: Optional[bool] = None

class RecommendationResponse(BaseModel):
    id: str
    title: str
    description: Optional[str]
    image_url: str
    target_type: str
    target_value: str
    sort_order: int
    is_active: bool
    created_at: datetime
    updated_at: datetime

    class Config:
        from_attributes = True

# ==================== 生成请求（更新）====================

# 继承原有，这里省略
```

---

## 6. 认证与安全设计

### 6.1 JWT 认证流程

```
用户登录
    ↓
验证密码
    ↓
生成 Access Token (24小时)
生成 Refresh Token (7天，存入数据库)
    ↓
返回 Token 对
    ↓
后续请求携带 Access Token
    ↓
Token 过期 → 使用 Refresh Token 获取新 Token
    ↓
登出 → 撤销 Refresh Token
```

### 6.2 注册登录流程

#### 注册流程

```
1. 用户填写：用户名 + 邮箱 + 密码
2. 后端验证：
   - 用户名是否已存在
   - 邮箱是否已存在
   - 密码强度校验
3. 创建用户记录
4. 如果启用邮箱验证 → 发送验证邮件
5. 自动登录，返回 Token
```

#### 登录流程

```
1. 用户输入：登录名（邮箱或用户名）+ 密码
2. 查找用户：根据邮箱或用户名
3. 验证密码
4. 记录最后登录时间
5. 检查是否需要强制修改密码
6. 生成并返回 Token
7. 记录审计日志
```

### 6.3 配额扣减（并发安全）

```python
# 使用数据库行锁防止并发超扣

with db.begin():
    user = db.query(User).filter(User.id == current_user.id).with_for_update().first()

    if user.quota_used >= user.quota_total:
        raise HTTPException(status_code=400, detail="配额不足")

    user.quota_used += 1
    user.last_activity_at = datetime.utcnow()

    # 创建任务...
```

### 6.4 密码安全

- 使用 bcrypt 哈希（work factor = 12）
- 密码历史记录（默认保留最近 5 个，不允许重复使用）
- 密码强度验证（可配置）

### 6.5 防暴力破解

```python
# 内存存储登录尝试（单机版）
# 5 次失败 → 锁定 5 分钟

login_attempts: Dict[str, List[float]] = {}
lockout_until: Dict[str, float] = {}
```

---

## 7. 前端设计

### 7.1 路由设计

```tsx
// frontEnd/src/routes/index.tsx

<BrowserRouter>
  <AuthProvider>
    <Routes>
      {/* 公开路由 */}
      <Route path="/login" element={<LoginPage />} />
      <Route path="/register" element={<RegisterPage />} />
      <Route path="/forgot-password" element={<ForgotPasswordPage />} />
      <Route path="/reset-password" element={<ResetPasswordPage />} />
      <Route path="/verify-email" element={<VerifyEmailPage />} />

      {/* 受保护路由 */}
      <Route path="/" element={<ProtectedRoute><App /></ProtectedRoute>} />

      {/* 后台管理 */}
      <Route path="/admin" element={<AdminRoute><AdminLayout /></AdminRoute>}>
        <Route index element={<AdminDashboard />} />
        <Route path="users" element={<AdminUsers />} />
        <Route path="users/:id" element={<AdminUserDetail />} />
        <Route path="recommendations" element={<AdminRecommendations />} />
      </Route>
    </Routes>
  </AuthProvider>
</BrowserRouter>
```

### 7.2 认证状态管理

```typescript
// frontEnd/src/context/auth-context.tsx

interface AuthUser {
  id: string;
  username: string;
  email: string;
  role: 'user' | 'admin';
  quota_total: number;
  quota_used: number;
  email_verified: boolean;
  require_password_change: boolean;
}

interface AuthContextType {
  user: AuthUser | null;
  isLoading: boolean;
  login: (login: string, password: string) => Promise<void>;
  register: (username: string, email: string, password: string) => Promise<void>;
  logout: () => void;
  refreshUser: () => Promise<void>;
}
```

### 7.3 主要页面

#### 注册页 `/register`
- 用户名输入
- 邮箱输入
- 密码输入 + 强度指示器
- 确认密码
- 提交按钮
- 跳转到登录

#### 登录页 `/login`
- 邮箱/用户名输入
- 密码输入
- 登录按钮
- 跳转到注册
- 忘记密码链接

#### 前台改造
- 顶部导航 → 增加用户头像/下拉菜单
- 显示配额进度条（已用/总数）
- 个人资料入口
- 退出登录
- 首页推荐图轮播

---

## 8. 邮箱验证（可选）

### 8.1 配置说明

```env
# 开发环境可以关闭
REQUIRE_EMAIL_VERIFICATION=false

# 生产环境配置邮件服务
REQUIRE_EMAIL_VERIFICATION=true
SMTP_HOST=smtp.qq.com
SMTP_PORT=587
SMTP_USER=your-qq@qq.com
SMTP_PASSWORD=your-authorization-code
FROM_EMAIL=your-qq@qq.com
```

### 8.2 降级方案

如果未配置邮件服务：
- 自动跳过验证步骤
- `email_verified` 直接设为 `true`
- 用户注册后直接可用

---

## 9. 实施步骤

### Phase 0: MySQL 基础设施
1. 安装 MySQL 8.0+，创建数据库
2. 更新 `requirements.txt`，添加依赖
3. 配置 Alembic，生成并执行迁移
4. SQLite 数据迁移（可选）

### Phase 1: 后端认证核心
1. 创建数据模型
2. 实现 JWT 工具和密码安全
3. 实现认证接口（注册/登录/刷新/登出）
4. 实现用户接口
5. 更新生成接口：认证 + 配额扣减
6. 初始化数据脚本

### Phase 2: 后端管理功能
1. 实现管理接口（用户/推荐图/统计）
2. 审计日志
3. 防暴力破解

### Phase 3: 前端认证
1. 安装 React Router，配置路由
2. 实现 Auth Context
3. 实现登录/注册页面
4. 改造前台主应用
5. Token 自动刷新

### Phase 4: 后台管理 UI
1. 管理布局
2. 仪表盘
3. 用户管理
4. 推荐图管理

---

## 10. requirements.txt 更新

```txt
fastapi>=0.100.0
uvicorn>=0.23.0
sqlalchemy>=2.0.0
pymysql>=1.1.0
cryptography>=41.0.0
alembic>=1.12.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.6
pydantic-settings>=2.0.0
pydantic>=2.0.0
slowapi>=0.1.9
limits>=3.5.0
# 邮件服务（可选）
# aiosmtplib>=2.0.0
# email-validator>=2.0.0
```

---

## 11. 初始化数据

```python
# 默认管理员
用户名: admin
邮箱: admin@example.com
密码: admin123
配额: 9999
首次登录强制修改密码: 是

# 测试用户
用户名: test
邮箱: test@example.com
密码: test123
配额: 20
```

---

## 12. 错误码设计

| 错误码 | HTTP 状态 | 说明 |
|--------|-----------|------|
| `AUTH_UNAUTHORIZED` | 401 | 未认证 |
| `AUTH_TOKEN_EXPIRED` | 401 | Token 已过期 |
| `AUTH_TOKEN_INVALID` | 401 | Token 无效 |
| `AUTH_FORBIDDEN` | 403 | 无权限 |
| `AUTH_ACCOUNT_DISABLED` | 403 | 账号已禁用 |
| `AUTH_ACCOUNT_LOCKED` | 429 | 账号已锁定 |
| `QUOTA_INSUFFICIENT` | 400 | 配额不足 |
| `USER_NOT_FOUND` | 404 | 用户不存在 |
| `USER_ALREADY_EXISTS` | 409 | 用户已存在 |
| `VALIDATION_ERROR` | 422 | 验证失败 |
| `AUTH_UNAUTHORIZED` | 401 | 未认证 |
| `AUTH_FORBIDDEN` | 403 | 无权限 |
